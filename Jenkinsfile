pipeline {
    agent any 
    stages {
        stage('Build') {
            steps {
                echo 'Build with MyDevOpsAcademy'
                // sh 'date'
            }
        }
        
        }
    stages {
        stage('Test') {
            steps {
                echo 'Test with MyDevOpsAcademy'
                // sh 'date'
            }
        }
        
    }
    stages {
        stage('Deploy') {
            steps {
                echo 'Deploy with MyDevOpsAcademy'
                // sh 'date'
            }
        }
        
    }
}
