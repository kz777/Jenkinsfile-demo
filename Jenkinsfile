pipeline {
    agent any 
    stages {
        stage('Build') {
            steps {
                echo 'Build with MyDevOpsAcademy'
                // sh 'date'
            }
        }
        stage('Test') {
            steps {
                echo 'Test with MyDevOpsAcademy'
                // sh 'date'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy with MyDevOpsAcademy'
                // sh 'date'
            }
        }
        
    }
}
