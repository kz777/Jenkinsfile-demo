pipeline {
    agent any 
    stages {
        stage('Example Build') {
            steps {
                echo 'Build with MyDevOpsAcademy'
                sh 'date'
            }
        }
        stage('Example Test') {
            steps {
                echo 'Test with MyDevOpsAcademy'
                sh 'date'
            }
        }
    }
}
